(ns webgl.hello-fragment
  (:gen-shader
   :program :fragment
   :uniforms [[float width]
              [float height]]
   :varyings [[vec4 v-color]]))

(defn -main
  []
  (let [[x y z w] (gl-frag-coord)]
    (set! (gl-frag-color) v-color)))
