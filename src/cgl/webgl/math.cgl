(ns webgl.math)

(def pi 3.14159265)

(defn rotate
  [^vec4 pos ^float theta]
  (let [[x y z w] pos
        theta (radians theta)]
    (vec4 (- (* x (cos theta)) (* y (sin theta)))
          (+ (* x (sin theta)) (* y (cos theta)))
          z 1.0)))
